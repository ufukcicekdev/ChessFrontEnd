// Re-export from context so existing imports keep working
export { useChallenges } from "@/context/ChallengesContext";
export type { PendingChallenge, SentChallenge } from "@/context/ChallengesContext";
